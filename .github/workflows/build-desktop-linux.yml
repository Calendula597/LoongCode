name: build-desktop-linux

on:
  workflow_dispatch:
    inputs:
      channel:
        description: "Build channel"
        required: false
        type: choice
        default: prod
        options:
          - prod
          - beta
          - dev

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Read version from package.json
        id: version
        shell: bash
        run: |
          v=$(node -p "require('./packages/loongcode/package.json').version")
          echo "version=$v" >> "$GITHUB_OUTPUT"
          echo "将使用版本: $v"

      - uses: ./.github/actions/setup-bun

      - name: Install Linux deps
        run: |
          sudo apt-get update
          sudo apt-get install -y rpm

      - name: Build
        run: bun run build
        working-directory: packages/desktop
        env:
          LOONGCODE_CHANNEL: ${{ inputs.channel }}

      - name: Package
        run: npx electron-builder --linux --publish never --config electron-builder.config.ts
        working-directory: packages/desktop
        env:
          LOONGCODE_CHANNEL: ${{ inputs.channel }}

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: loongcode-desktop-linux-x64
          path: |
            packages/desktop/dist/*.tar.gz
            packages/desktop/dist/*.deb
            packages/desktop/dist/*.rpm
          if-no-files-found: ignore

      - name: Upload to Release
        shell: bash
        env:
          GH_REPO: ${{ github.repository }}
          GH_TOKEN: ${{ github.token }}
        working-directory: packages/desktop/dist
        # 传安装包 + latest*.yml（electron-updater 检查更新需要）
        run: gh release upload "v${{ steps.version.outputs.version }}" *.tar.gz *.deb *.rpm latest*.yml --clobber --repo "$GH_REPO" || echo "(无文件或上传失败，看上面日志)"
